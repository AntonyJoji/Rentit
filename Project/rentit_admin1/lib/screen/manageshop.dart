import 'package:flutter/material.dart';

class Manageshop extends StatelessWidget {
  const Manageshop ({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("manageshop"),
    );
  }
}