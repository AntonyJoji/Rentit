import 'package:flutter/material.dart';

class deliveryboy extends StatelessWidget {
  const deliveryboy({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("deliveryboy"),
    );
  }
}