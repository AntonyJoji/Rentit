import 'package:flutter/material.dart';

class manageProduct extends StatelessWidget {
  const manageProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("manageproduct"),
    );
  }
}